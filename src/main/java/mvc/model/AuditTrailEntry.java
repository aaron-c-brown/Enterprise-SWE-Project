package mvc.model;

public class AuditTrailEntry {

    // Will be used to store each Audit Trail Entries field values, minus the ID

    // Unused... Was going to store the values for the audit gui but why recreate the wheel
    // whenever we have AuditTrail.java instead.

}
